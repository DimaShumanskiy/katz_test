# Price Alert Feature - FSD Implementation

Полнофункциональная система оповещений о ценах токенов, реализованная по методологии Feature-Sliced Design (FSD).

## 🏗️ Архитектура

Проект следует методологии [Feature-Sliced Design](https://feature-sliced.design/), обеспечивая:
- Модульность и масштабируемость
- Четкое разделение ответственности
- Легкую поддержку и тестирование

### Структура папок

```
src/
├── app/                    # App layer - точки входа и конфигурация
├── entities/               # Entities layer - бизнес-сущности
│   └── price-alert/
│       ├── types.ts        # Интерфейсы PriceAlert, ValidationState
│       └── model/
│           └── priceAlertStore.ts  # Zustand store с persist
├── features/               # Features layer - законченные фичи
│   └── price-alert/
│       ├── ui/             # UI компоненты
│       │   ├── PriceAlertScreen.tsx
│       │   ├── ConditionToggle.tsx
│       │   ├── ValidatedInput.tsx
│       │   ├── WarningBadge.tsx
│       │   └── SaveButton.tsx
│       ├── model/
│       │   └── formModel.ts    # Локальный state формы
│       └── lib/
│           └── validation.ts   # Функции валидации
└── shared/                 # Shared layer - переиспользуемый код
    ├── ui/                 # UI-kit
    │   ├── Button.tsx
    │   ├── Input.tsx
    │   └── Toggle.tsx
    ├── lib/
    │   ├── storage.ts      # AsyncStorage wrapper
    │   └── format.ts       # Утилиты форматирования
    └── constants/
        └── mockData.ts     # Моковые данные
```

## 🚀 Технологии

- **React Native** + **Expo** - фреймворк для мобильной разработки
- **TypeScript** - статическая типизация
- **Zustand** - легковесный state management
- **AsyncStorage** - локальное хранилище данных

## 📦 Установка

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm start

# iOS
npm run ios

# Android
npm run android
```

## 🎯 Функциональность

### 1. Выбор условия оповещения
- **Цена выше** - оповестить когда цена превысит целевое значение
- **Цена ниже** - оповестить когда цена опустится ниже целевого значения

### 2. Ввод целевой цены
- Нормализация ввода (поддержка различных форматов: `1,234.56` или `1.234,56`)
- Валидация в реальном времени
- Визуальная обратная связь (errors, warnings)

### 3. Сохранение настроек
- Персистентное хранилище через AsyncStorage
- Автоматическое сохранение и восстановление при перезапуске
- Список всех сохраненных оповещений

## 🧪 Валидация

### Правила валидации

| Сценарий | Результат | Сообщение |
|----------|-----------|-----------|
| Пустое поле | ❌ Error | "Введите цену" |
| Некорректное число | ❌ Error | "Некорректное число" |
| Отрицательное значение | ❌ Error | "Цена должна быть > 0" |
| < 0.0001 | ❌ Error | "Цена должна быть > 0.0001" |
| \|target - current\| < 1% | ⚠️ Warning | "Цена близка к текущей (±X%)" |
| Корректное значение | ✅ Valid | - |

### Примеры нормализации

```typescript
normalizePriceInput("1,234.56") // → "1234.56"
normalizePriceInput("1.234,56") // → "1234.56"
normalizePriceInput("abc123")   // → "123"
normalizePriceInput("")         // → ""
```

## 🔍 Типы данных

### PriceAlert

```typescript
interface PriceAlert {
  id: string;                     // Уникальный идентификатор
  tokenSymbol: string;            // Символ токена (BTC, ETH, etc.)
  condition: 'above' | 'below';   // Условие оповещения
  targetPrice: number;            // Целевая цена
  createdAt: Date;                // Дата создания
}
```

### ValidationState

```typescript
interface ValidationState {
  isValid: boolean;               // Валидность значения
  error?: string;                 // Сообщение об ошибке
  warning?: string;               // Предупреждение
  normalizedValue: string;        // Нормализованное значение
}
```

## 🧩 Компоненты

### Shared Layer

#### Button
Переиспользуемая кнопка с различными вариантами (`primary`, `secondary`, `danger`).

```typescript
<Button 
  onPress={handlePress} 
  disabled={false}
  loading={false}
  variant="primary"
>
  Сохранить
</Button>
```

#### Input
Текстовое поле с поддержкой валидации.

```typescript
<Input
  label="Целевая цена"
  value={value}
  onChangeText={setValue}
  error="Введите цену"
  warning="Цена близка к текущей"
/>
```

#### Toggle
Переключатель с несколькими опциями.

```typescript
<Toggle
  options={[
    { value: 'above', label: 'Цена выше' },
    { value: 'below', label: 'Цена ниже' }
  ]}
  value={condition}
  onChange={setCondition}
/>
```

### Feature Layer

#### PriceAlertScreen
Главный экран фичи, объединяющий все компоненты.

#### ConditionToggle
Выбор условия оповещения (выше/ниже).

#### ValidatedInput
Поле ввода целевой цены с валидацией.

#### WarningBadge
Бейдж для отображения предупреждений.

#### SaveButton
Кнопка сохранения с автоматическим disabled состоянием.

## 💾 State Management

### Global State (entities/price-alert/model/priceAlertStore.ts)

Zustand store для управления списком оповещений с persist middleware:

```typescript
const { alerts, addAlert, removeAlert } = usePriceAlertStore();
```

**Actions:**
- `addAlert(alert)` - добавить новое оповещение
- `removeAlert(id)` - удалить оповещение
- `updateAlert(id, updates)` - обновить оповещение
- `loadAlerts()` - загрузить оповещения
- `clearAlerts()` - очистить все оповещения

### Local State (features/price-alert/model/formModel.ts)

Локальный state для управления формой:

```typescript
const { condition, targetPrice, validation, setCondition, setTargetPrice, reset } = useFormModel();
```

**Actions:**
- `setCondition(condition)` - установить условие
- `setTargetPrice(value, currentPrice)` - установить и валидировать цену
- `reset()` - сбросить форму

## 🧪 Тестирование

### TypeScript компиляция

```bash
npx tsc --noEmit
```

### Сценарии тестирования

1. **Пустое поле**
   - Ввести: ``
   - Ожидание: Error "Введите цену"

2. **Некорректные символы**
   - Ввести: `abc123def`
   - Ожидание: Нормализация в `123`

3. **Различные форматы**
   - Ввести: `1,234.56` → `1234.56`
   - Ввести: `1.234,56` → `1234.56`

4. **Отрицательное число**
   - Ввести: `-100`
   - Ожидание: Error "Цена должна быть > 0"

5. **Warning при близкой цене**
   - Текущая цена: `43250`
   - Ввести: `43100` (разница 0.35%)
   - Ожидание: Warning "Цена близка к текущей"

6. **Сохранение и персистентность**
   - Создать оповещение
   - Перезапустить приложение
   - Проверить: оповещение сохранено

## 📋 Критерии оценки

| Критерий | Вес | Статус |
|----------|-----|--------|
| Структура FSD | 25% | ✅ Выполнено |
| State management (Zustand + persist) | 25% | ✅ Выполнено |
| TypeScript типизация | 20% | ✅ Выполнено |
| UI компоненты | 15% | ✅ Выполнено |
| Валидация | 15% | ✅ Выполнено |

## 🎨 UI/UX особенности

- Современный Material Design + iOS стиль
- Адаптивная верстка
- Интерактивная валидация
- Понятные сообщения об ошибках
- Подтверждение удаления
- Успешные уведомления

## 🔧 Конфигурация

### Mock данные (shared/constants/mockData.ts)

```typescript
export const MOCK_TOKEN_PRICES = {
  BTC: 43250,
  ETH: 2340,
  SOL: 98.5,
  USDT: 1.0,
  BNB: 312,
};
```

### AsyncStorage ключ

```typescript
name: 'price-alert-storage'
```

## 📝 Принципы FSD

1. **Слои импортируют только вниз**
   - `app` → `features` → `entities` → `shared`
   
2. **Изоляция слайсов**
   - `features/price-alert` не импортирует другие features
   
3. **Shared - переиспользуемый код**
   - UI компоненты без бизнес-логики
   
4. **Entities - бизнес-сущности**
   - Типы + store для PriceAlert
   
5. **Features - законченные фичи**
   - UI + валидация + локальный state

## 🚀 Дальнейшее развитие

### Возможные улучшения

- [ ] Unit-тесты (Jest)
- [ ] E2E тесты (Detox)
- [ ] Множественные токены
- [ ] Реальное API (CoinGecko)
- [ ] Push-уведомления
- [ ] Максимум 10 алертов на токен
- [ ] Редактирование существующих алертов
- [ ] История срабатывания алертов
- [ ] Темная тема

## 📄 Лицензия

MIT

## 👤 Автор

Тестовое задание: React Native Developer
